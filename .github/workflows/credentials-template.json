{
  "_comment": "This is a template for ADMIN_CENTER_API_CREDENTIALS GitHub secret",
  "_instructions": [
    "1. Replace the placeholder values with your actual credentials",
    "2. DO NOT commit this file with real credentials",
    "3. Copy the final JSON (without comments) to GitHub Secrets",
    "4. GitHub: Settings → Secrets and variables → Actions → New repository secret",
    "5. Name: ADMIN_CENTER_API_CREDENTIALS",
    "6. Value: Paste the JSON below (without _comment and _instructions fields)"
  ],
  "_remove_before_use": "Remove all fields starting with underscore before using",

  "tenantId": "YOUR_AZURE_AD_TENANT_ID",
  "clientId": "YOUR_APPLICATION_CLIENT_ID",
  "clientSecret": "YOUR_CLIENT_SECRET_VALUE",
  "scopes": "https://api.businesscentral.dynamics.com/.default"
}
